import 'package:flutter/material.dart';

class ChartsPages extends StatelessWidget {
  const ChartsPages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Charts Page'),
    );
  }
}
